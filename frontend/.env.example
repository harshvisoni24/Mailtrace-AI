# MAILTRACE AI — Frontend environment
# NEVER put GEMINI_API_KEY or any secret key here — this file is bundled into
# client-side JavaScript and is publicly visible in the browser.
VITE_API_BASE_URL=http://localhost:5000/api
